struct vec2 {
  int x;
  int y;
};

struct vec2 vec2_add(struct vec2 a, struct vec2 b)
{
  struct vec2 c;
  c.x = a.x + b.x;
  c.y = a.y + b.y;
  return c;
}

int program()
{
  struct vec2 a;
  a.x = 3;
  a.y = 4;
  
  struct vec2 b;
  b = a;
  
  struct vec2 c;
  c = vec2_add(a, b);
  
  print_int c.x;
  print_int c.y;
}
